# Маркетинговая Аналитическая Платформа

Веб-платформа для анализа маркетинговых данных из Яндекс.Директ с использованием Google Sheets в качестве базы данных.

## 🚀 Возможности

- **Дашборд** - Обзор KPI: лиды, целевые, квалифицированные, продажи, CPL, CPO
- **Менеджер лидов** - Интерактивная таблица с редактированием статуса и комментариев
- **Отчёты** - Агрегация по кампаниям с экспортом в Excel
- **Импорт Excel** - Загрузка новых данных из еженедельных отчётов
- **Рекомендации** - Автоматический анализ эффективности кампаний

## 📋 Требования

- Node.js 18+
- Google Cloud Service Account с доступом к Google Sheets API
- Существующая Google таблица с данными лидов

## ⚙️ Установка

1. Клонируйте репозиторий:
```bash
git clone <repository-url>
cd marketing_platform
```

2. Установите зависимости:
```bash
npm install
```

3. Создайте файл `.env` на основе `.env.example`:
```bash
cp .env.example .env
```

4. Заполните переменные окружения:
```env
GOOGLE_SHEET_ID=1HUpmF9aG3UD9blHHsIhWQ8NPnPTeW36USgkrRVSh5Tg
GOOGLE_CLIENT_EMAIL=your-service-account@project.iam.gserviceaccount.com
GOOGLE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----"
```

5. Запустите dev сервер:
```bash
npm run dev
```

Откройте [http://localhost:3000](http://localhost:3000)

## 📊 Структура Google Sheets

Платформа ожидает следующую структуру листа "Январь 2026":

| Колонка | Описание |
|---------|----------|
| № | Номер лида |
| Кампания | Название кампании (РСЯ, МК, Поиск и т.д.) |
| Дата | Дата в формате YYYY-MM-DD |
| Время | Время в формате HH:MM:SS |
| Квалификация | Статус лида (недозвон, СПАМ, целевой, квал, продажа) |
| Комментарий | Текст комментария |
| ПРОДАЖИ | Флаг продажи (1 или пусто) |

## 🔧 Настройка Service Account

1. Создайте проект в [Google Cloud Console](https://console.cloud.google.com)
2. Включите Google Sheets API
3. Создайте Service Account (APIs & Services → Credentials)
4. Создайте ключ JSON для Service Account
5. Скопируйте `client_email` и `private_key` из JSON в `.env`
6. Предоставьте доступ Service Account к вашей таблице (Доступ → Поделиться)

## 🚢 Деплой на Google Cloud Run

```bash
# Авторизация
gcloud auth login

# Деплой
gcloud run deploy marketing-platform \
  --source . \
  --region=europe-west1 \
  --allow-unauthenticated \
  --set-env-vars="GOOGLE_SHEET_ID=...,GOOGLE_CLIENT_EMAIL=...,GOOGLE_PRIVATE_KEY=..."
```

## 🛠 Технологии

- **Next.js 14** - React фреймворк с App Router
- **TypeScript** - Типизация
- **Tailwind CSS** - Стилизация
- **Shadcn/UI** - UI компоненты
- **Recharts** - Графики
- **Google Sheets API** - База данных
- **xlsx** - Парсинг Excel файлов

## 📁 Структура проекта

```
├── app/
│   ├── api/           # API маршруты
│   ├── leads/         # Страница лидов
│   ├── reports/       # Страница отчётов
│   ├── settings/      # Страница настроек
│   └── page.tsx       # Дашборд
├── components/        # React компоненты
├── lib/              # Утилиты и коннекторы
└── types/            # TypeScript типы
```

## 📝 Лицензия

MIT
